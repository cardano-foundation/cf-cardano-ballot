image:
  repository: pro.registry.gitlab.metadata.dev.cf-deployments.org/base-infrastructure/docker-registry/voting-ledger-follower-app
  tag: "0.0.1"
  pullPolicy: Always

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""


service:
  type: ClusterIP
  port: 9090

ingress:
  enabled: true

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

monitoringEnabled: true

diagnosticMode:
  enabled: false

corsAllowedOrigins: https://{{ .Values.domain }}
springProfiles: dev--preprod
bindOnEventIds: CIP-1694_Pre_Ratification_4619

## The list of desired instances and the schema name
## they'll connect too.
## One instance = 1 deployment w/ one pod (replicas: 1) using 1 schema.
instances:
  - name: ""
    schemaName: ""

yaci:
  startBlockHash: ca71d7941c316561c3b21d0f557f34d97a61f97a333c18ed5f2800d74b64e377
  startSlotNumber: "32349482"
